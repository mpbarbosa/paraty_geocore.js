# Enforce the `engines` field in package.json at install time.
# npm install fails immediately if the active Node.js or npm version
# does not satisfy the declared range, preventing subtle runtime issues.
engine-strict=true
